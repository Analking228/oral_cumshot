events {
    worker_connections   1024;
}

http {

    server {
        listen 80 _;
        listen [::]:80 _;
        location / {
            root /data/www;
        }
        location /images/ {
            root /data;
        }
    }
}